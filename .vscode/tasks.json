{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BUILD",
            "type": "shell",
            "command": "mvn clean exec:exec package",
            "group": "build"
        }
    ]
}